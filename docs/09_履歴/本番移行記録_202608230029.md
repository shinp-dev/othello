# 本番移行記録 `202608220029_daily_rating_snapshot.sql`

> **履歴資料:** これは2026-08-23に実施した本番移行の記録です。現在の実行手順ではありません。現行の運用方法は[日次レートスナップショット運用](../04_レーティング/日次レートスナップショット運用.md)を参照してください。

確認日: 2026-08-23 JST

この資料は、ちゃんりばの「前日順位」を本番Supabaseへ導入した際の事前確認（preflight）、マイグレーション適用、初回スナップショット、Supabase Cron設定、事後検証の履歴です。現在状態の正本は[運用マップ](../01_全体設計/運用マップ.md)、設計と現行運用は[日次レートスナップショット運用](../04_レーティング/日次レートスナップショット運用.md)を参照してください。

## 対象と基準

- Supabase プロジェクト: `othello`
- プロジェクト ref: `zgzllmaoyymoeiqtybck`
- データベース region: Northeast Asia (Tokyo), `ap-northeast-1`
- リポジトリ: `shinp-dev/othello`
- 本番導入基準コミット: `86a7d334716c5e924c6b2d6ff73a148b990abdca`
- 開始時の端末内 `HEAD` / `origin/main`: 一致
- 開始時working tree: clean
- GitHub Actions CI: 実行 `32608987199`、success

CIでは管理ワーカーのCI / typecheck / テスト、Androidのテスト / lint / debug / リリース / AAB、境界 / SQL セキュリティ / リリース検証、端末内 Supabase起動とpgTAPが成功していました。

## 本番事前確認（preflight）

2026-08-23 12:32 JST前後に本番SQL Editorから読み取り専用 catalog 問い合わせを実行し、次を確認しました。

- `profiles.last_active_at`、`touch_last_active()`、`queue_expired_account_deletions()`が存在し、027適用済み状態と一致。
- `rating_daily_snapshot`と`refresh_rating_daily_snapshot(date)`は不在で、029は未適用。
- 同名更新 関数のoverloadは0件。
- `profiles` / `rating_history`の029依存列、`auth.uid()`、`anon` / `authenticated` / `service_role`が存在。
- `pg_cron` extensionと`cron.job`は不在。既存Cronとの重複なし。
- `supabase_migrations.schema_migrations`は不在。

本番にマイグレーション 履歴 テーブルがないため`supabase db push`は使用せず、029だけを明示的に適用しました。028は本番へ適用しておらず、今後も永久欠番・適用禁止です。

## 029適用

リポジトリの`supabase/migrations/202608220029_daily_rating_snapshot.sql`全文を`begin;` / `commit;`で囲み、Supabase Dashboard SQL Editorから1トランザクションで実行しました。結果は`Success. No rows returned.`で、追加SQLや場当たり的な修正SQLは実行していません。

適用後の読み取り専用 catalog監査結果:

- テーブル `public.rating_daily_snapshot`: 所有者 `postgres`、RLS 有効。
- 列: `user_id uuid`、`snapshot_date date`、`rank integer`、`active_user_count integer`、`top_percentile numeric(8,4)`。全列がNOT NULL。
- 主 キー、プロフィール 外部 キー、rank / アクティブユーザー件数 / percentile check 制約、日付インデックスが存在。
- ポリシー `users read own daily rating snapshot`: SELECT、`auth.uid() = user_id`。
- テーブル ACL: `authenticated`はSELECTのみ。INSERT / UPDATE / DELETE不可。`anon` / `PUBLIC`はSELECT不可。
- 関数 `public.refresh_rating_daily_snapshot(date)`: 所有者 `postgres`、`SECURITY DEFINER`、`search_path=""`、デフォルト引数1個。
- 関数 ACL: `service_role`はEXECUTE可。`PUBLIC` / `anon` / `authenticated`はEXECUTE不可。
- 関数定義は`rating_history`とAsia/Tokyo、`RANK()`を使用し、`last_active_at`を使用しない。
- 既存`ratings`、`rating_history`、`enqueue_or_match()`、`submit_match_result(uuid,text,text,text,text,jsonb)`、`finalize_match_v2(uuid)`は存在。

029は既存テーブル / RPC / RLS 契約を変更しないadditive マイグレーションです。旧クライアントが使う現在レート、matchmaking、結果 submissionのスキーマ / 関数を置換していません。本番テスト対局やダミーデータ作成は行わず、既存契約の存在とACLを読み取り専用で確認しました。

## 初回スナップショット

実行直前のAsia/Tokyo日時は2026-08-23 12:36、期待されるスナップショット日は`2026-08-22`、テーブルは0行でした。デフォルト引数で`select public.refresh_rating_daily_snapshot();`を1回実行し、5行を生成しました。

集計検証:

- スナップショット日: `2026-08-22`だけ
- 行 件数 / アクティブユーザー 件数: 5
- rank: 1〜5
- top percentile: 20.0000〜100.0000
- invalid rank / 件数 / percentile: 0件
- 削除ユーザー混入: 0件
- 本番`rating_history`から同じJST 締切、30日半開区間、締切前最新レート、`RANK()`、percentile式で再計算した結果との差分: 0件
- 同日再試行の戻り値: 5
- 再試行前後の行 件数: 5 / 5
- 再試行前後の集計 fingerprint: 不変

検証では個人のUUID、レート、rankを列挙せず、件数・値域・差分数・fingerprintだけを確認しました。

## Supabase Cron

初回スナップショットの検証後、Supabase Dashboardのデータベース Extensionsから`pg_cron` 1.6.4を`pg_catalog`へ有効化しました。有効化直後に`cron.job`と`cron.job_run_details`の存在、ジョブ総数0、同名ジョブ 0、同コマンド ジョブ 0を確認してから1件だけ登録しました。

- ジョブ名: `daily-rating-snapshot`
- ジョブID: `1`
- 有効な: `true`
- スケジュール: `10 15 * * *`
- UTC: 毎日15:10
- JST: 毎日00:10（翌日、DSTなし）
- コマンド: `select public.refresh_rating_daily_snapshot();`
- データベース: `postgres`
- execution 実行主体: `postgres`
- Dashboard next 実行: `24 Aug 2026 00:10:00 (+0900)`

登録後の`cron.job`は総数1、同名または同コマンドに一致するジョブも1で、重複はありません。Cronと同じデータベース 所有者、同じSQLを手動で再実行し5を返すことを確認しました。これは同日再試行のためスナップショットを変更しません。

## 監視

初回定期実行は2026-08-24 00:10 JSTです。本番導入直後はまだ定期実行が発生していないため、`cron.job_run_details`のジョブID 1の履歴は0件でした。

実行成否は次で確認します。

1. Supabase Dashboard → Integrations → Cron → Jobs → `daily-rating-snapshot`の実行履歴。
2. 同画面の`View Cron logs`。
3. `cron.job_run_details`で`jobid = 1`の`status`、`return_message`、`start_time`、`end_time`を読み取り専用確認。
4. Postgres ログで関数 エラーを確認。
5. `rating_daily_snapshot`のdateがAsia/Tokyoの前日、日付が1種類、rank / 件数 / percentileが有効範囲であることを集計 問い合わせで確認。

Cronが停止・失敗してもAndroidは古い・当日・取得失敗のスナップショットを「前日順位」として表示せず、端末内最高も更新しません。現在レートとアプリ全体はスナップショットの有無から独立しています。

## 巻戻し・復旧上の注意

- ジョブの異常時は最初に`daily-rating-snapshot`を無効化し、追加実行を止めます。他のCronは変更しません。
- 同じスナップショット日の再試行は既存結果を変更せず、より新しいスナップショットがある状態で古いdateへ巻き戻す実行は関数が拒否します。
- 029を取り消すdrop SQLは今回作成・実行していません。テーブル / 関数削除はAndroidの新クライアント 契約を失わせるため、影響確認と所有者承認なしに行いません。
- 本番にマイグレーション 履歴 テーブルがない状態は継続しています。CLI マイグレーション管理への基準点移行は別作業です。

## 本番で変更したもの

- 029が定義する`rating_daily_snapshot`、インデックス、RLS / ポリシー / ACL、更新 関数 / ACL。
- `2026-08-22`の初回スナップショット 5行。
- `pg_cron` 1.6.4 extension。
- Supabase Cron ジョブ `daily-rating-snapshot` 1件。

## 変更していないもの

- 028およびその他のマイグレーション。
- レート algorithm、`ratings`、`rating_history`、レート 履歴 保持。
- matchmaking、結果 submission、Research、Auth設定、既存RLS / RPC、その他Cron。
- Cloudflare、Google Play、Android バージョン、LP。
- 本番テストアカウント、ダミー対局、既存ユーザーデータ。

## 未確認事項

- 初回定期実行の実履歴と成功結果。2026-08-24 00:10 JST以後に確認する。
- Android端末 / emulatorが接続されていなかったため、本番AccountScreenでの表示はこの作業では未確認。Androidの単体テスト / lint / ビルドとCIは基準コミットで成功済み。
- 実ユーザーのログイン、対局、結果 submissionを伴う本番E2Eは、安全な読み取り専用検証の範囲を超えるため実施していない。
